{
  "name": "ts-helloworld",
  "version": "0.0.0",
  "private": true,
  "description": "Fixture: minimal TypeScript-SAPUI5 app — detection + TS-aware discovery (V1.9 Phase 0). NOT the e2e-real fixture.",
  "scripts": {
    "build": "ui5 build",
    "ts-typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@openui5/types": "^1.120.0",
    "@types/qunit": "^2.19.0",
    "@ui5/cli": "^3.9.0",
    "@ui5/linter": "^1.0.0",
    "typescript": "^5.0.0",
    "ui5-tooling-transpile": "^3.0.0"
  }
}
